info:
  name: User_login
  type: http
  seq: 2

http:
  method: POST
  url: "{{BASE_URL}}/auth/login"
  body:
    type: json
    data: |
      body: { 
        email: " ", 
        password: " ",
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
