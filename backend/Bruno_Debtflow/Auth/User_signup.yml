info:
  name: User_signup
  type: http
  seq: 1

http:
  method: POST
  url: "{{BASE_URL}}/auth/signup"
  body:
    type: json
    data: |-
      {
        "username": "JohnDoe",
        "email": "JohnDoe@gmail.com",
        "password": "123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
