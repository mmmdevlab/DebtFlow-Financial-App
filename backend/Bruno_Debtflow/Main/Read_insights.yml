info:
  name: Read_insights
  type: http
  seq: 2

http:
  method: GET
  url: "{{BASE_URL}}/insights"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
