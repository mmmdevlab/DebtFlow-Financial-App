info:
  name: Read_dashboard
  type: http
  seq: 6

http:
  method: GET
  url: "{{BASE_URL}}/dashboard"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
