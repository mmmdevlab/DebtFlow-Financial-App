info:
  name: Update_editPayment
  type: http
  seq: 3

http:
  method: PUT
  url: "{{BASE_URL}}/api/payments/:id"
  params:
    - name: id
      value: PASTE_PAYMENT_ID_HERE
      type: path
  body:
    type: json
    data: |-
      {
        "amount": 750,
        "payment_date": "2026-04-11"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
