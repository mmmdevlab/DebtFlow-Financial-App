info:
  name: Update_editPayment
  type: http
  seq: 3

http:
  method: PUT
  url: "{{BASE_URL}}/debts/:debtId/payments/:paymentId"
  params:
    - name: paymentId
      value: xxx
      type: path
    - name: debtId
      value: xxx
      type: path
  body:
    type: json
    data: |-
      body: { 
        amount_paid: " ", 
        payment_date: " ", 
        notes: " ",
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
