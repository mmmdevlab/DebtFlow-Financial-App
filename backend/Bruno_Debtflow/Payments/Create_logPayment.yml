info:
  name: Create_logPayment
  type: http
  seq: 2

http:
  method: POST
  url: "{{BASE_URL}}/api/payments"
  body:
    type: json
    data: |-
      {
        "user_id": "PASTE_USER_ID_HERE",
        "debt_id": "PASTE_DEBT_ID_HERE",
        "amount": 500,
        "payment_date": "2026-04-11"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
