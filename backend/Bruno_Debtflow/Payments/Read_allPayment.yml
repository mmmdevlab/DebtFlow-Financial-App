info:
  name: Read_allPayment
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASE_URL}}/debts/:debtId/payments"
  params:
    - name: debtId
      value: xxx
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
